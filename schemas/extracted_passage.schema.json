{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://research-pdf-vault.local/schemas/extracted_passage.schema.json",
  "title": "ExtractedPassage",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "passage_id",
    "paper_id",
    "instance_id",
    "source_location",
    "text",
    "support_tag"
  ],
  "properties": {
    "schema_version": {
      "const": "1.0.0"
    },
    "passage_id": {
      "type": "string",
      "pattern": "^passage_[a-z0-9_]+$"
    },
    "paper_id": {
      "type": "string",
      "readOnly": true,
      "pattern": "^paper_[a-z0-9_]+$"
    },
    "instance_id": {
      "type": "string",
      "pattern": "^instance_[a-z0-9_]+$"
    },
    "source_location": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "page",
        "start_offset",
        "end_offset"
      ],
      "properties": {
        "page": {
          "type": "integer",
          "minimum": 1
        },
        "start_offset": {
          "type": "integer",
          "minimum": 0
        },
        "end_offset": {
          "type": "integer",
          "minimum": 1
        }
      }
    },
    "text": {
      "type": "string",
      "minLength": 1
    },
    "support_tag": {
      "enum": [
        "supports",
        "contradicts",
        "mixed",
        "context"
      ]
    }
  }
}
